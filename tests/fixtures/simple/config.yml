version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: Build
          command: echo "Building..."

workflows:
  main:
    jobs:
      - build
